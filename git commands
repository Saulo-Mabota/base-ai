
                yarn start:dev
                yarn dev
                npx prisma generate
         

 # Write your query or mutamutaiontion here
mutation{
#seederCreateRole{name} 
# seederCreatePermissions{
#     id
#     name
#   }
#seederCreateUser{username} 
#seederCreateRole{name}
  login(loginInput:{
   email:"admin@mbm.com"
    password:"12345678"
  }){
    user{
      id
     }
     access_token
   }
}

read-app
list-users
list-role
list-department
list-category
list-subcategory
list-product
list-stock

git branch // Show all branches
git branch -a // Show all branches (local and remote)
git status // Show the status of your working directory

WORKING ON YOUR BRANCH (DEV-SAULO):
1.	git add .
2.	git commit -m "Short, meaningful message"
3.	git push origin dev-saulo
UPDATING YOUR BRANCH FROM MAIN:
1.	git checkout main
2.	git pull origin main
3.	git checkout dev-saulo
4.	git merge main
UPDATING THE MAIN BRANCH:
1.	git checkout main
2.	git pull origin main


git checkout -b dev-saulo // Create a new branch called dev-saulo
git push origin dev-saulo // Feed the new branch to the remote repository

